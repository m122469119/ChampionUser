<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="?attr/actionBarSize"
              android:background="@color/main_app_color"
              android:orientation="vertical"
              android:theme="@style/AppTheme.AppBarOverlay">

    <RelativeLayout
        android:id="@+id/screen_rel_title_content"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize">

        <ImageView
            android:id="@+id/screen_left_btn"
            android:layout_width="?attr/actionBarSize"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentLeft="true"
            android:background="@drawable/selector_toolbar_home_icon_bg"
            android:scaleType="centerInside"
            android:src="@drawable/app_bar_back" />

        <LinearLayout
            android:id="@+id/screen_layout_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:clickable="true">

            <TextView
                android:id="@+id/tv_screen_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:ellipsize="end"
                android:maxEms="10"
                android:lines="1"
                android:textColor="@color/app_bar_text_color"
                android:textSize="@dimen/textsize_medium"/>


        </LinearLayout>


    </RelativeLayout>

</LinearLayout>